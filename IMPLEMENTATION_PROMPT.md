# Codex向けプロンプト：エクスポート駆動の数学学習Webアプリ

あなたはこのリポジトリで、数学学習用の承認済み問題を使った選択形式の学習Webアプリを実装してください。計画やモックだけで終わらず、動作するアプリ、テスト、運用ドキュメントまで完成させてください。

## 最初に行うこと

1. リポジトリ全体、`AGENTS.md`、既存のREADME、package scriptsを確認する。
2. 次の実データとSchemaを読み、推測ではなく実際の形式に合わせる。
   - `dist/latest.json`
   - `dist/<content-version>/manifest.json`
   - `dist/<content-version>/index.json`
   - `dist/<content-version>/questions/*.json`
   - `schema/public-question.schema.json`
   - `schema/content-export-index.schema.json`
   - `schema/content-export-manifest.schema.json`
3. 既存のWebアプリがあればその構成・技術を尊重する。なければ、`apps/learner-web/`にReact、TypeScript、Viteを使って新設する。
4. 既存の問題制作、検証、エクスポート処理を壊さない。問題JSONそのものは、明確な不整合がない限り編集しない。

## 目標

高校数学の問題を、途中式ごとに「次に行う操作」を選びながら解く日本語Webアプリを作る。

アプリのコードへ問題やコンテンツバージョンを埋め込んではならない。静的に公開されたエクスポートファイルを実行時に読み込み、次のファイルを追加・差し替えるだけで問題を更新できるようにする。

```text
content/
  latest.json
  2026-07-20.1/
    manifest.json
    index.json
    questions/
      <question-id>.json
```

新しいリリースでは、古いバージョンのディレクトリを残したまま新しいバージョンを追加し、最後に`latest.json`だけを切り替える。Webアプリの再ビルドを必須にしない。

## 必須アーキテクチャ

UIとコンテンツ取得を直接結合せず、少なくとも次の責務を分離する。名称はプロジェクトに合わせて変更してよい。

```text
UI
 └─ Learning session / state machine
     ├─ ContentRepository interface
     │   ├─ HttpContentRepository
     │   └─ CachedContentRepository
     └─ ProgressRepository interface
         └─ BrowserProgressRepository
```

- `ContentRepository`
  - 最新版の確認
  - manifestとindexの取得
  - 問題の遅延取得
  - Schemaバージョン確認
  - SHA-256検証
  - 最後に正常だったコンテンツへのフォールバック
- 学習セッション
  - 問題、現在のstep、選択結果、完了状態を管理
  - Reactコンポーネントの表示都合から独立したロジックにする
- `ProgressRepository`
  - ブラウザ内に学習進捗を保存
  - 将来サーバー保存へ交換できる境界を設ける

コンテンツのベースURLはビルド時に固定せず、実行時設定ファイル、環境変数のフォールバック、または同等の仕組みで変更可能にする。既定値は同一オリジンの`/content/`でよい。URLの連結には`new URL()`を使い、相対パスと末尾スラッシュを正しく処理する。

## コンテンツ更新の動作

起動時に次の順序で処理する。

1. `latest.json`を`cache: "no-store"`で取得する。
2. 対応する`manifest.json`を取得する。
3. アプリが対応する`schemaVersion`であることを確認する。初期対応版は`1.0.0`。
4. `index.json`を取得し、manifest記載のSHA-256とバイト数を検証する。
5. indexの基本構造と`questionCount`の整合を検証する。
6. すべて成功してから、そのコンテンツバージョンを利用可能として切り替える。
7. 問題JSONは選択された時点で取得し、同様にSHA-256、バイト数、最低限の構造を検証する。

更新途中、通信失敗、JSON破損、未対応Schema、ハッシュ不一致のときは、不完全な新バージョンへ切り替えない。最後に正常利用できたバージョンがブラウザキャッシュにあればそれを使い、なければ再試行可能な日本語エラー画面を表示する。

バージョン付きファイルは長期キャッシュ可能、`latest.json`はキャッシュしないという配信設定を、READMEに具体例付きで記載する。

## 学習画面の要件

### 問題選択

- 単元、難易度、タグで絞り込める。
- indexだけで一覧を構成し、一覧表示のために全問題JSONを先読みしない。
- 問題数、現在のコンテンツバージョンを表示する。
- 未着手、途中、完了が分かる。

### 問題プレイヤー

- `problemText`と`problemLatex`を適切に表示する。
- 数式はKaTeXなどの実績あるライブラリで表示する。
- 現在の`solutionStep`について、`beforeLatex`と選択肢を表示する。
- 選択肢は`choice.id`を保持したまま、挑戦開始時に一度だけシャッフルする。再レンダーのたびに順番を変えない。現在データでは正解位置に偏りがあっても、UIから推測できないようにする。
- 不正解時はその選択肢の`incorrectReason`を表示し、同じstepで再選択できる。
- 正解時は`afterLatex`と`reason`を表示し、利用者の操作で次のstepへ進む。
- 最終step完了後に`answer.finalAnswerLatex`と`solutionPlan.summary`を表示する。
- 解答前に正解や最終解答をUIへ露出しない。ただし、これは自主学習用であり、配信済みJSONを解析すれば正解を読めるという脅威モデルでよい。
- 最初からやり直す操作を用意する。

### 進捗

- 進捗キーは最低でも`question.id + question.revision`を含める。
- contentVersionが変わってもrevisionが同じ問題の完了状態は維持する。
- 同じidでもrevisionが変わった問題は、新しい問題として未着手にする。
- 保存データの破損時にアプリ全体が起動不能にならないようにする。

## UI品質

- 日本語の学習画面として、落ち着いて読みやすいデザインにする。
- スマートフォンを主対象にしつつ、PCでも不自然に横へ伸びないレスポンシブ構成にする。
- 色だけで正誤を伝えない。
- キーボード操作、フォーカス表示、適切な見出し、ARIA liveによる正誤通知を備える。
- `prefers-reduced-motion`を尊重する。
- ローディング、空一覧、通信失敗、データ破損、未対応バージョンの各状態を用意する。
- 生のHTMLを問題データから挿入しない。

## 実装上の制約

- 問題ID、単元、コンテンツバージョン、選択肢数をハードコードしない。
- 正解が常に`choice-a`、選択肢が常に3個などと仮定しない。
- `index.json`内の`file`および`latest.json`内の`manifest`を信頼してURL解決するが、`..`や別オリジンへの逸脱を拒否する。
- manifestに存在しない問題ファイルを取得しない。
- 取得したレスポンスのテキストに対してハッシュを検証してからJSONとして採用する。
- コンテンツ取得失敗を握りつぶさず、利用者向けメッセージと開発者向け診断情報を分ける。
- 不要なバックエンド、認証、管理画面、分析基盤は今回作らない。
- Viteのビルド出力で、リポジトリ直下のコンテンツエクスポート用`dist/`を上書きしない。

## コンテンツ配置と開発体験

以下を整備する。

- ローカル開発で既存の`dist/latest.json`以下を読み込める仕組み
- 本番では`/content/`または設定した外部URLから取得する仕組み
- エクスポート済みコンテンツをWeb公開ディレクトリへ同期する安全なnpm script
- コピー先を限定し、問題制作側の`dist/`を削除・上書きしないこと
- CORSが必要な外部配信を使う場合の設定説明

実装方式は、Viteの開発サーバープラグイン、限定的な同期スクリプト、または同等に単純で安全な方法から、既存構成に最も合うものを選ぶ。

## テスト要件

最低限、次を自動テストする。

1. `latest → manifest → index → question`のURL解決。
2. 未対応`schemaVersion`の拒否。
3. indexまたは問題のSHA-256不一致の拒否。
4. 新版の取得失敗時に旧版を維持すること。
5. `question.id + revision`による進捗分離。
6. 正解、不正解、再選択、次step、完了までの状態遷移。
7. 選択肢をシャッフルしても`choice.id`で正誤判定できること。
8. 現在の実エクスポート30問を読み込める統合テスト。

テスト、型検査、lint、production buildを実行し、失敗があれば修正してすべて通す。ブラウザで主要画面を実際に確認し、スマートフォン幅とPC幅のスクリーンショットまたは同等の視覚確認を行う。

## 完了条件

- `latest.json`の切り替えだけで新しいコンテンツ版を検出できる。
- アプリの再ビルドなしに、静的コンテンツファイルの追加・差し替えで更新できる。
- 更新が壊れていても、正常だった旧版を利用し続けられる。
- 30問を単元・難易度・タグから選び、全stepを選択形式で最後まで学習できる。
- リロード後もrevision単位の進捗が残る。
- 問題制作リポジトリの内部レビュー情報はUIにもキャッシュにも入らない。
- READMEに、開発方法、ビルド方法、コンテンツ更新方法、キャッシュ方針、障害時の切り戻し方法が記載されている。

## 作業の進め方と最終報告

- 不明点はまずリポジトリと実データから調査し、安全な範囲では合理的な判断で進める。
- 大きな設計変更が必要な場合だけ、理由と選択肢を示して確認する。
- 既存のユーザー変更を保持し、今回と無関係なファイルを変更しない。
- 最終報告では、実装した構成、コンテンツ更新手順、検証結果、残る制約を簡潔に示す。
